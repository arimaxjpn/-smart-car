# Smart Car プロジェクト共通ルール

このファイルは Claude と Codex の両方が参照する共通ルールです。

---

## プロジェクト概要

業務用車両の乗車・降車を記録し、Googleスプレッドシートに自動保存するPWAアプリ。

---

## 技術スタック

- フロントエンド: HTML/CSS/JavaScript（`index.html` 単一ファイル）
- バックエンド: Google Apps Script（`Code.gs`）
- ホスティング: GitHub Pages（フロントエンド）、GAS（バックエンド）

---

## 開発ルール

- GAS への fetch は `Content-Type: text/plain` を使うこと（application/json だと CORSエラーになる）
- UIルールは `DESIGN.md` に従うこと（フォント・カラー・ボタン形状・カード・スペーシング）
- テスト中はアプリタイトル横にバージョンを表示する
  - ファイル先頭付近に `const _VERSION = "v1.0"` のように定義する
  - デプロイ前に手動でバージョンを上げる（v1.1 → v1.2 など）

---

## 運用フロー

このプロジェクトは以下の流れで作業を進める：

```
ユーザー → Claude（設計・指示書の作成）
                ↓
        CODEX_TASK.md に指示を書き込む
                ↓
           Codex（実装・コード変更）
                ↓
        ユーザー確認 → Claude（レビュー・完了処理）
```

---

## CODEX_TASK.md の運用ルール

- **役割**: Claude が作成した指示書。Codex への作業指示を記載する。
- **Codex は作業開始時に必ず `CODEX_TASK.md` を読むこと。**
- **Codex は作業完了後に `CODEX_TASK.md` の内容を空にすること。**
- **`CODEX_TASK.md` が空の場合は何もしない。次の指示を待つ。**
- 作業履歴はここに残さない。履歴管理は Claude が担当する。

## フィードバック時の対応ルール

- ユーザーからの曖昧なフィードバック（「もう少し〜」「ここが違う」など）に対しては、コードを書く前に現状の値や設定を確認する質問をすること。
- 方向性が合っていても、数値・パラメータの変更は必ずユーザーに現状を確認してから実施する。

---

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | フロントエンド |
| `Code.gs` | Google Apps Script バックエンド |
| `RULES.md` | 本ファイル。ClaudeとCodex共通のプロジェクトルール |
| `DESIGN.md` | UIデザインルール |
| `CODEX_TASK.md` | Claudeが書くCodexへの指示書（完了後リセット） |
